name: infrastructure

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Placeholder (Nomad disabled)
    runs-on: ubuntu-24.04
    steps:
      - name: Skip
        run: echo "Nomad CI temporarily disabled"
