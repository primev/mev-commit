apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "p2p.fullname" . }}-test-connection"
  labels:
    {{- include "p2p.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "p2p.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
