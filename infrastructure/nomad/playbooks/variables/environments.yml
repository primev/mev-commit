environments:
  devenv:
    chain_id: 17864
    profile: "{{ profile }}"
    version: "{{ version }}"
    secrets: generate
    domain: localhost
  testenv:
    chain_id: 17864
    profile: "{{ profile }}"
    version: "{{ version }}"
    secrets: fetch
    genesis_timestamp: "0x668ebdff"
    domain: testnet.mev-commit.xyz
