environments:
  devenv:
    chain_id: 17864
    profile: "{{ profile }}"
    version: "{{ version }}"
    secrets: generate
  testenv:
    chain_id: 17864
    profile: "{{ profile }}"
    version: "{{ version }}"
    secrets: fetch
    genesis_timestamp: "0x66700e3f"
    domain: testnet.mev-commit.xyz
