job "eth-faucet" {
  datacenters = ["dc1"]

  group "eth-faucet-group" {
    count = 1

    network {
      mode = "bridge"

      dns {
        servers = ["1.1.1.1"]
      }
      # TODO: Figure out which job already uses 8080
      port "http" {
        static = 8080
      }
    }

    task "eth-faucet" {
      driver = "exec"

      service {
        name = "eth-faucet"
        port = "http"
        tags = ["http"]
        provider = "nomad"
      }

      artifact {
        source = "https://github.com/chainflag/eth-faucet/releases/download/v1.1.0/eth-faucet_1.1.0_Linux_x86_64.tar.gz"
        destination = "eth-faucet"
      }

      artifact {
        source = "https://primev-infrastructure-artifacts.s3.us-west-2.amazonaws.com/UTC--2024-05-28T06-17-56.991113000Z--9a5a8ebd13951c8500c728c1982317a503b3e8ce"
      }

      template {
        data = <<-EOH
          #!/usr/bin/env bash
          # TODO: Use url of locally running chain
          export RPC_URL="https://chainrpc.testnet.mev-commit.xyz/"
          export KEYSTORE="UTC--2024-05-28T06-17-56.991113000Z--9a5a8ebd13951c8500c728c1982317a503b3e8ce"
          cd eth-faucet
          KEYSTORE_PASSWORD="{{ with secret "secret/data/mev-commit" }}{{ .Data.data.faucet_keystore_password }}{{ end }}"
          echo $KEYSTORE_PASSWORD > password.txt
          chmod +x eth-faucet
          ./eth-faucet --wallet.keyjson $KEYSTORE --wallet.keypass password.txt --wallet.provider $RPC_URL
        EOH
        destination = "local/run.sh"
        perms = "0755"
      }

      config {
        command = "bash"
        args = ["-c", "local/run.sh"]
      }
    }
  }
}
