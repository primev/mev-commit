apiVersion: v1
kind: Service
metadata:
  name: {{ include "relay-indexer.fullname" . }}
  labels:
    {{- include "relay-indexer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "relay-indexer.selectorLabels" . | nindent 4 }}
